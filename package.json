{
  "name": "NodeFS",
  "version": "1.0.0",
  "description": "",
  "main": "nodeJS-sdk-test.js",
  "dependencies": {
    "node-jose": "^1.0.0",
    "object-assign": "^4.1.0",
    "superagent": "^4.0.0"
  },
  "scripts": {
    "build": "npm build",
    "clean": "rm -rf node_modules",
    "reinstall": "npm run clean && npm install",
    "rebuild": "npm run clean && npm install && npm run build"
  }
}
